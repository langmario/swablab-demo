---
import { Picture } from 'astro:assets';
import heroImage from '@/assets/hero.png';
import Link from '@/components/ui/link.astro';
---

<section class:list={['grid place-items-center py-4 lg:grid-cols-2', Astro.props.class]}>
  <div class='hidden py-6 md:order-1 md:block'>
    <Picture
      src={heroImage}
      alt='Astronaut in the air'
      widths={[200, 400, 600]}
      sizes='(max-width: 800px) 100vw, 620px'
      loading='eager'
      format='avif'
    />
  </div>
  <div>
    <h1 class='font-headings text-5xl font-bold lg:text-6xl lg:tracking-tight xl:text-7xl xl:tracking-tighter'>
      Deine offene Werkstatt mitten im Schwarzwald
    </h1>
    <p class='mt-4 max-w-xl text-lg text-slate-600'>
      Egal ob du Profi bist oder einfach mal reinschnuppern willst - bei uns findest du Werkzeuge, Wissen und coole
      Leute. Komm vorbei und leg los!
    </p>
    <div class='mt-6 flex flex-col gap-3 sm:flex-row'>
      <Link href='/mitmachen'> Jetzt mitmachen </Link>
    </div>
  </div>
</section>
